<%= form_with(model: webhook_target) do |form| %>
  <% if webhook_target.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(webhook_target.errors.count, "error") %> prohibited this webhook_target from being saved:</h2>

      <ul>
        <% webhook_target.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :repository, style: "display: block" %>
    <%= form.text_field :repository %>
  </div>

  <div>
    <%= form.submit 'Submit', class: 'btn btn-primary', type: 'button' %>
  </div>
<% end %>
